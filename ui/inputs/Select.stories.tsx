import React from "react";

import Select from "./Select";

export const base = () => (
  <Select placeholder="Base select">
    <option value="Some value">Some value</option>
  </Select>
);

export default {title: "UI/Inputs/Select"};
